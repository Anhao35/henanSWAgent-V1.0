# 运行追踪、任务意图、报告模板改造

## 基线

2026-09-04 开始。保留既有未提交改动。备份位于项目同级 `implementation-backup-20260904`，包含原源码、原可执行 JAR 及升级前数据库 SQL。数据库备份含敏感数据，只应保留在受控本机，不要提交 Git 或公开分享。

## 实施约束

- 保留 SIR 确定性 IOC 提取；意图控制执行，不删除原始链接。
- 前端过程与答案分离；不模拟百分比，不把心跳冒充上游进展。
- 运行事件与报告均按用户/会话鉴权；仅保存允许的诊断摘要，禁止原始 HTTP 请求头或模型推理。
- Dify 导出为新版本，不直接覆盖已发布工作流；切换前完成配置及回归。
- 数据库仅追加 Flyway 迁移，不删除已有账号或会话。

## 工作项

- [x] 平台真实节点事件、流式快捷查询、历史过程、断线恢复查看与停止请求
- [x] 每轮任务模式、Dify 意图门导出、保留 SIR 输出和跨模式上游会话隔离
- [x] 证据快照、版本化模板、报告保存/导出
- [x] 隔离数据库接口验收、前端构建、模拟接口浏览器验收、图结构检查
- [x] 本机平台后端启动、V6 数据库增量升级及未登录接口拦截检查
- [ ] 用户更新失效的 SIR 调试 Key，重启 SIR
- [ ] 在 Dify 导入并发布新工作流，配置 DIFY_ROUTED_API_KEY 后进行真实全链路验收

具体启动、使用、限制及回退说明见 `UPGRADE-运行追踪与报告.md`。上述模拟上游测试不等于真实 Dify 联调成功。
