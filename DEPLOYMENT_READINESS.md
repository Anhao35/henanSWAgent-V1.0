# 部署就绪审计（2026-09-07）

## 已就绪

- Vue 生产构建、Nginx SPA/长连反代、静态资源缓存和基础安全响应头。
- Spring Boot Java 17 镜像、Flyway V1–V7、MySQL、Redis Session、MinIO、Actuator 健康检查。
- 登录名/已验证邮箱/已验证手机登录；Argon2 密码；CSRF、CORS、Secure Cookie 生产配置。
- 注册与找回：邮箱 6 位/手机 4 位码，SHA-256+服务端密钥储存，10 分钟 TTL、60 秒重发间隔、5 次尝试、一次性消费、定时清理。
- V2.5 兜底 ATT&CK 确定性意图门和显式 LocalAttackMCP 工具节点。
- LocalAttack Bridge v0.2 固定回归，正式/潜在/内容候选/无映射四级语义分离。
- 生产 Compose 、健康依赖、持久化卷、密钥模板、启停/升级/备份指引。
- 已用隔离项目名和独立端口完成全新生产栈冒烟：MySQL、Redis、MinIO、Bridge、后端、Web 全部 healthy，Web、API 反代和 Bridge 均返回 200。
- 密钥文件、数据卷、日志、构建产物和用户上传内容均排除出 Git。

## 上线前仍需外部环境验收

| 级别 | 事项 | 验收标准 |
|---|---|---|
| 阻断 | 在生产 Dify 导入并发布 V2.5 | 4 组 V2.5 手工回归全部通过 |
| 阻断 | 生产 SMTP 与短信网关 | 真实邮箱/手机可收到验证码，响应不含 devCode |
| 阻断 | HTTPS 证书、域名和防火墙 | 只开放 443，Cookie 带 Secure，8081/8011 不对公网 |
| 阻断 | 全部曾暴露 API Key/账据轮换 | Git 历史与服务器镜像无有效密钥 |
| 高 | 备份恢复演练 | MySQL/MinIO/Dify 从备份恢复到新环境成功 |
| 高 | 并发和长耗时压测 | 目标并发下无会话串扰、重复提交、连接泄漏 |
| 高 | 宿主机资源定容 | Dify+向量库+模型调用+平台有 CPU/内存/磁盘安全余量 |
| 中 | 50 GB 知识库金标试点 | 准确率、无关召回率、P95 延迟达标后再扩容 |

## 结论

代码与部署工程已达到「可进入预生产验收」级别，不应在未完成上表阻断项时宣称「已达生产上线」。
